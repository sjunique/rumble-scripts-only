using UnityEngine;

 
// SpawnedByPathPopulator.cs
using UnityEngine;

// Tag component added to items spawned by the path populator.
// Lets us clear only our own instances without touching hand-placed items.
public class SpawnedByPathPopulator : MonoBehaviour { }


 
