using UnityEngine;
 

// Add this to collectible prefabs/instances so the controller can count them exactly.
public class CollectibleMarker : MonoBehaviour { }
// CollectibleMarker.cs (attach to collectible prefabs/instances)
